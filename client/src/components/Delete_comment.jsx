//import deleteComment from "../../../server/controllers/commentCtrl.js"

const Delete_comment = ({username, post_id, comment_id}) => {
    let post = document.getElementById(post_id)
    let comment = document.getElementById(comment_id)
    return(console.log(0)
        //deleteComment
        //post.comments.filter(comm => comm !== comment)
    )
}

export default Delete_comment